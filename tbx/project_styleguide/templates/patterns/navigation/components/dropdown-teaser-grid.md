# Teaser grid dropdown

Desktop dropdown panel using a card grid layout. Used for **Sectors** with division pages or manually curated teaser links.

Each card supports a title, description, optional tags (shown with middle dots), and an accent colour theme.
